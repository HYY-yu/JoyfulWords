# API 配置
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# 应用配置
PORT=8190
NEXT_PUBLIC_APP_URL=http://localhost:8190

# === OpenTelemetry 配置 ===
# 服务名称
OTEL_SERVICE_NAME=joyful-words-frontend
  
# Next.js OTel 详细日志 (仅开发环境)
NEXT_OTEL_VERBOSE=1

# === 客户端追踪配置 ===
# 启用客户端追踪 (浏览器端)
NEXT_PUBLIC_ENABLE_TELEMETRY=true

# 客户端 traces 发送端点 (开发环境直接发送到 Jaeger)
# OTLP Trace Exporter Endpoint (开发环境使用 Jaeger)
# 启动 Jaeger: docker run -d -p 16686:16686 -p 4318:4318 jaegertracing/all-in-one:latest
# 访问 Jaeger UI: http://localhost:16686
NEXT_PUBLIC_OTEL_EXPORTER_URL=http://localhost:4318/v1/traces

# === 功能开关配置 ===
# doing: 开发中,显示 Coming Soon 页面
# show: 已上线,显示完整功能
NEXT_PUBLIC_FEATURE_IMAGE_GENERATION=doing
NEXT_PUBLIC_FEATURE_KNOWLEDGE_CARDS=doing
NEXT_PUBLIC_FEATURE_SEO_GEO=doing

# === Cookie Banner 配置 ===
# 在所有环境启用 Cookie Banner (包括开发环境)
NEXT_PUBLIC_ENABLE_COOKIE_BANNER=true

# === 支付渠道配置 ===
# 控制哪些支付渠道在用户界面中显示
# 多个渠道用逗号分隔，例如: "paypal,oxapay"
# 如果不设置或设置为空，默认显示所有可用渠道
# 可用值: paypal, oxapay
NEXT_PUBLIC_ENABLED_PAYMENT_PROVIDERS=paypal,oxapay

# 注意：复制此文件为 .env.local 并填入真实凭据
